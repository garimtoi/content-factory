# Google OAuth 2.0 Settings
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# JWT Secret Key (auto-generated if not set)
AUTH_SECRET_KEY=your-secret-key-here

# Admin emails (comma-separated, these users are auto-approved)
ADMIN_EMAILS=admin@example.com,admin2@example.com

# OAuth Redirect URI (must match Google Console setting)
# Google OAuth only allows localhost or valid domains (no IP addresses)
# Use localhost for OAuth callback
OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback

# Database paths
# 통합 DB 경로 (pokervod.db)
SHARED_DB_PATH=D:/AI/claude01/qwen_hand_analysis/data/pokervod.db
DATABASE_PATH=D:/AI/claude01/qwen_hand_analysis/data/pokervod.db
USERS_DB_PATH=D:/AI/claude01/archive-analyzer/data/output/archive.db

# MeiliSearch
MEILISEARCH_URL=http://localhost:7700
MEILISEARCH_API_KEY=archive-analyzer-dev-key

# SMB Settings
SMB_SERVER=10.10.100.122
SMB_SHARE=docker
SMB_USERNAME=GGP
SMB_PASSWORD=your-password
ARCHIVE_PATH=GGPNAs/ARCHIVE
